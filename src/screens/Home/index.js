import React from "react";
import Camera from "../../components/Camera";
import Messages from "../../components/Messages";

const Home = () => {
  return (
    <main>
      <Messages />
      <Camera />
    </main>
  );
};

export default Home;
